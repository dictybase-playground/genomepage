var React = require('react');

var ProteinLinkView = React.createClass({
    render: function() {
        return (
            <a href="#">Protein sequence, domains and much more ....</a>
        );
    }

});

module.exports = ProteinLinkView;
